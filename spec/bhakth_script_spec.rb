require 'spec_helper'

describe BhakthScript do
  it 'has a version number' do
    expect(BhakthScript::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
